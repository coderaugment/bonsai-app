{
  "name": "@bonsai/agent",
  "version": "0.0.1",
  "description": "Bonsai agent runtime — extracted from OpenClaw",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "bonsai": "./bin/bonsai.js"
  },
  "scripts": {
    "build": "tsc && npm run copy-bin",
    "copy-bin": "mkdir -p dist && cp -r bin dist/ || true",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^17.3.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "@vitest/ui": "^4.0.18",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
